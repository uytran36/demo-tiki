import React from 'react';
import { CommonConfig, TreeGraphData } from '../interface';
export interface DecompositionTreeGraphConfig extends Omit<CommonConfig, 'data'> {
    data: TreeGraphData;
}
declare const DecompositionTreeGraph: React.FC<DecompositionTreeGraphConfig>;
export default DecompositionTreeGraph;
